function calculate(){
    let morAmount = Number(document.getElementById('amount').value);
    let morTerm = Number(document.getElementById('term').value);
    let intRate = Number(document.getElementsById('rate').value);
    let morType = document.querySelector('input[name="mortgage"]:checked').value;


}